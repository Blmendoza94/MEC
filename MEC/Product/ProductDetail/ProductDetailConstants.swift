enum ProductDetailConstants {
    enum ViewController {
        static let title = "Detalle del producto"
    }

    enum Error {
        static let message = "Ha ocurrido un error al intentar obtener el detalle del producto"
    }
}
